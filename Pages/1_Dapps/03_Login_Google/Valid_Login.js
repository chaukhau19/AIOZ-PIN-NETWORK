import { commoncfg_Account, commoncfg_Locator, commoncfg_Verify } from "../../../Data/Common.js";
import { FunctionPage } from "../../Functions.js";

export class LoginPage {
    constructor(page) {
        this.page = page;
        this.functionPage = new FunctionPage(page);
    }



    async Login_Google(wallet) {
        try {
            await this.functionPage.gotoURL(commoncfg_Account.URL_Google);
            await this.functionPage.SignInGmail();

        } catch (error) {
            console.warn("Login Google failed, continuing...");
        } 
    }

    async Login_AIOZPin(wallet) {
        try {
            await this.functionPage.SignInAIOZPIN();
        } catch (error) {
            console.error("Sign In AIOZ PIN failed:", error.message || error);
        }        
    }



}